---
export interface Props {
  href: string;
}
const { href, target } = Astro.props;
---

<a class={href === Astro.url.pathname ? "active" : ""} href={href} target={target}><slot /></a>
<style>
  a { @apply block p-2 hover:bg-gray-200 }
  a.active { @apply bg-gray-100 }
</style>